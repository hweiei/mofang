# 更新记录

## v1.0.0

首个公开版本。

- 单文件程序：Windows / macOS / Linux，不用装 Node。
- `novel setup` 建书房，自动生成给 Claude Code、Codex、Cursor、Gemini CLI 的说明文件。
- `novel mcp`：MCP 服务，Claude Desktop 等客户端直接连。
- 小说全链路：灵感卡 → 选题 → 设定 → 卷纲 → 大纲 → 节拍表 → 正文（多稿）→ 读者试读 → 自改 → 选稿 → 账本。
- 短剧剧本线与三种分镜，Excel 拍摄表。
- 工作台 `novel ui`。
